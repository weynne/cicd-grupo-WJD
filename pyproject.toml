# ABOUTME: Project configuration for Ruff linting used by the CI pipeline.
# ABOUTME: Line-length checks are relaxed for the inline HTML/CSS templates.
[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B"]
ignore = ["E501"]
