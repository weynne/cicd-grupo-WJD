# ABOUTME: Development and CI dependencies for testing, linting and auditing.
# ABOUTME: Installed on top of requirements.txt in the CI pipeline.
-r requirements.txt
pytest==8.3.3
ruff==0.6.9
pip-audit==2.7.3
